---
- name: Create sssd config directory
  file:
    state: directory
    path: "{{ sssd_config_file | dirname }}"

- name: Configure sssd
  template:
    src: sssd.conf
    dest: "{{ sssd_config_file }}"
    owner: root
    group: root
    mode: u=rw,g=,o=
  notify: restart sssd

- name: Install sssd packages
  apt:
    name: "{{ sssd_packages }}"
    install_recommends: no
    state: present
  notify: restart systemd-logind

- name: Enable automatic home directory creation
  lineinfile:
    path: /etc/pam.d/common-session
    regexp: 'pam_mkhomedir\.so'
    line: "session required pam_mkhomedir.so skel=/etc/skel/ umask=0022"
    state: present
  when: sssd_mkhomedir

- name: Disable automatic home directory creation
  lineinfile:
    path: /etc/pam.d/common-session
    regexp: 'pam_mkhomedir\.so'
    state: absent
  when: not sssd_mkhomedir

- name: Ensure sssd service is started and enabled
  service:
    name: sssd
    state: started
    enabled: yes

- name: Ensure sssd sockets are started and enabled
  ansible.builtin.service:
    name: sssd-{{ item }}.socket
    state: started
    enabled: yes
  loop: "{{ sssd_config.sssd.services | difference(sssd_disabled_sockets) }}"
  when:
    - sssd_config.sssd.services is defined
    - sssd_enable_sockets
